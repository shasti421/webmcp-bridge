{
  "name": "@webmcp-bridge/core",
  "version": "0.1.0",
  "description": "Shared execution engine, selector resolver, healing pipeline, result capturer",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": { "build": "tsc", "test": "vitest run", "test:coverage": "vitest run --coverage" },
  "dependencies": { "js-yaml": "^4.1.0", "ajv": "^8.12.0", "zod": "^3.22.0" },
  "devDependencies": { "@types/js-yaml": "^4.0.9", "vitest": "^1.2.0", "typescript": "^5.3.0" }
}
