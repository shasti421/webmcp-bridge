/**
 * CaptureMode — Engineer UI for teaching the system.
 *
 * Shows:
 * - Detected fields and outputs on current page
 * - Proposed page definition (editable YAML preview)
 * - Record action button (watches engineer interaction)
 * - Validation replay button
 * - Save to local / publish to registry
 */
// TODO: Implement — see spec: docs/specs/extension-ui-spec.md
export function CaptureMode() {
  return null;
}
